<UserControl x:Class="HttpDebug.Views.TabBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Surface900}">
    <Grid Height="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Width="40" Height="40" Style="{StaticResource IconButton}"
                ToolTip="Toggle sidebar (Ctrl+J)" Click="ToggleSidebar_Click">
            <Path Data="M 0 0 H 14 M 0 5 H 14 M 0 10 H 14"
                  Stroke="{StaticResource TextSecondary}" StrokeThickness="2" />
        </Button>

        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Tabs}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="TabBorder"
                                Margin="1,4,1,0" Padding="10,6,6,6"
                                MinWidth="160" MaxWidth="240"
                                CornerRadius="6,6,0,0"
                                Background="Transparent" Cursor="Hand"
                                MouseLeftButtonDown="TabBorder_MouseLeftButtonDown">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Request.Method}"
                                           Style="{StaticResource MethodPill}"
                                           Foreground="{Binding Request.Method, Converter={StaticResource MethodToColor}}"
                                           Margin="0,0,8,0" />

                                <TextBlock Grid.Column="1" Text="{Binding DisplayName}"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />

                                <Button Grid.Column="2" Style="{StaticResource IconButton}"
                                        Padding="4" MinWidth="20" MinHeight="20"
                                        Click="CloseTab_Click">
                                    <TextBlock Text="✕" FontSize="11" Foreground="{StaticResource TextDim}" />
                                </Button>
                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource Surface950}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Column="2" Width="40" Height="40" Style="{StaticResource IconButton}"
                ToolTip="New tab (Ctrl+T)" Click="NewTab_Click">
            <TextBlock Text="+" FontSize="18" />
        </Button>
    </Grid>
</UserControl>
