<Window x:Class="HttpDebug.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="620" MinWidth="480" SizeToContent="Height"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource Surface950}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Target="{Binding ElementName=StoragePathBox}" Padding="0,0,0,8"
               Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold"
               Content="Data folder (history and collections)" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="StoragePathBox" Grid.Column="0" MinHeight="34"
                     AutomationProperties.Name="Data folder" VerticalContentAlignment="Center"
                     HorizontalScrollBarVisibility="Auto" />
            <Button Grid.Column="1" Click="Browse_Click" Style="{StaticResource IconButton}"
                    Width="36" Height="34" Margin="8,0,0,0" ToolTip="Choose folder"
                    AutomationProperties.Name="Choose folder">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="16"
                           Foreground="{StaticResource TextSecondary}" />
            </Button>
        </Grid>
        <TextBlock x:Name="ErrorText" Grid.Row="2" Visibility="Collapsed"
                   Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,10,0,0"
                   AutomationProperties.LiveSetting="Assertive" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" IsCancel="True" Style="{StaticResource GhostButton}" MinWidth="80" Margin="0,0,8,0" />
            <Button x:Name="SaveButton" Content="Save" IsDefault="True" Click="Save_Click"
                    Style="{StaticResource PrimaryButton}" MinWidth="80" />
        </StackPanel>
    </Grid>
</Window>