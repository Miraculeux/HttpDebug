<Window x:Class="HttpDebug.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="HttpDebug"
        Width="1280" Height="800"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="#0a0f1a">

    <Grid>
        <!-- Loading overlay -->
        <Border x:Name="LoadingOverlay" Background="#0a0f1a" Panel.ZIndex="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="⚡ HttpDebug"
                           FontSize="28" FontWeight="Bold"
                           Foreground="#60a5fa"
                           HorizontalAlignment="Center" Margin="0,0,0,16" />
                <TextBlock x:Name="StatusText"
                           Text="Starting server..."
                           FontSize="14"
                           Foreground="#9ca3af"
                           HorizontalAlignment="Center" Margin="0,0,0,12" />
                <ProgressBar IsIndeterminate="True"
                             Width="200" Height="4"
                             Foreground="#3b82f6"
                             Background="#1e293b"
                             BorderThickness="0" />
            </StackPanel>
        </Border>

        <!-- Error overlay -->
        <Border x:Name="ErrorOverlay" Background="#0a0f1a" Panel.ZIndex="1" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Failed to start server"
                           FontSize="18" FontWeight="SemiBold"
                           Foreground="#f87171"
                           HorizontalAlignment="Center" Margin="0,0,0,8" />
                <TextBlock x:Name="ErrorText"
                           Text=""
                           FontSize="12"
                           Foreground="#9ca3af"
                           HorizontalAlignment="Center" Margin="0,0,0,16"
                           TextWrapping="Wrap" MaxWidth="400" />
                <Button x:Name="RetryButton"
                        Content="Retry"
                        Click="RetryButton_Click"
                        Padding="16,8"
                        Background="#3b82f6"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontSize="13" />
            </StackPanel>
        </Border>

        <!-- WebView2 -->
        <wv2:WebView2 x:Name="WebView"
                      Visibility="Collapsed" />
    </Grid>
</Window>
